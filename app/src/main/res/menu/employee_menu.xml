<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:icon="@drawable/ic_menu" android:orderInCategory="100"
        android:title="Option Menu" app:showAsAction="always">
        <menu>
            <item android:id="@+id/itemHistory" android:icon="@drawable/ic_history"
                android:title="History" />
            <item android:id="@+id/itemReminder" android:icon="@drawable/ic_reminder"
                android:title="Reminder" />
            <item android:id="@+id/itemLogout" android:icon="@drawable/ic_logout"
                android:title="Logout" />
        </menu>
    </item>

</menu>